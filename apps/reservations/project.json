{
  "name": "reservations",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/reservations/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/reservations",
        "main": "apps/reservations/src/main.ts",
        "tsConfig": "apps/reservations/tsconfig.app.json",
        "assets": ["apps/reservations/src/assets"],
        "webpackConfig": "apps/reservations/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "reservations:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "reservations:build:development"
        },
        "production": {
          "buildTarget": "reservations:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/reservations/jest.config.ts"
      }
    }
  },
  "tags": []
}
